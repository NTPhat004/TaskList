@model SubTaskModel

<li class="list-group-item d-flex justify-content-between align-items-center shadow-sm border rounded-pill @(Model.IsCompleted ? "bg-light" : "")" style="margin:5px 10px;" data-subtask-id="@Model.Id">
    @Html.Partial("_SubTaskList",Model)
</li>
